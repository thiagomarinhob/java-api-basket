package com.basket.api.domain.useCase.player;

import com.basket.api.domain.entity.Player;
import com.basket.api.domain.useCase.UseCase;

public interface CreatePlayerUseCase extends UseCase<Player, Player> {
}
